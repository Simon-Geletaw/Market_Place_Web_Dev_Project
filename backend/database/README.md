# Database Folder

This folder is reserved for database artifacts such as future migrations, seeds, or local export files.
The authoritative schema for this project currently lives in Documentation/database_schema.sql.
